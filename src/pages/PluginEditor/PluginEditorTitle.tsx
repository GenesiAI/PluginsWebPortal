import { Typography } from "@mui/material";
import { TitleTheme } from "theme";

const PluginEditorTitle = () => (
  <Typography variant="h2" sx={TitleTheme}>
    Create A Plugin
  </Typography>
);

export default PluginEditorTitle;
